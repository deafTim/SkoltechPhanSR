# Bitrate-SR pipeline deps (pip).
# Usage on Zhores (after: conda activate /home/timofey.glukhikh/.conda/science_phan):
#
#   # Prefer CUDA build matching the node driver (example: CUDA 12.4):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
#   pip install -r requirements.txt
#
# If torch/torchvision already installed with CUDA, skip the first line.

compressai
torchsr
transformers
pillow
matplotlib
numpy
tqdm
scikit-image
